export const SIDE_BAR_MINI_WIDTH = 48;
export const SIDE_BAR_SHOW_TIT_MINI_WIDTH = 80;

export const CONTENT_ENUM = {
    // auto width
    FULL: 'full',
    // fixed width
    FIXED: 'fixed',
}

// menu theme enum
export const THEME_ENUM = {
    DARK: 'dark',
    LIGHT: 'light'
}

export const SETTING_BUTTON_POSITION_ENUM = {
    AUTO: 'auto',
    HEADER: 'header',
    FIXED: 'fixed',
}

export const SESSION_TIMEOUT_PROCESSING_ENUM = {
    ROUTE_JUMP: 'ROUTE_JUMP',
    PAGE_COVERAGE: 'PAGE_COVERAGE'
}

/**
 * 权限模式
 */
export const PERMISSION_MODE_ENUM = {
    // role
    // 角色权限
    ROLE: 'ROLE',
    // back
    // 后端
    BACK: 'BACK',
    // route mapping
    // 路由映射
    ROUTE_MAPPING: 'ROUTE_MAPPING',
}

// Route switching animation
// 路由切换动画
export const ROUTER_TRANSITION_ENUM = {
    ZOOM_FADE: 'zoom-fade',
    ZOOM_OUT: 'zoom-out',
    FADE_SIDE: 'fade-slide',
    FADE: 'fade',
    FADE_BOTTOM: 'fade-bottom',
    FADE_SCALE: 'fade-scale',
}
